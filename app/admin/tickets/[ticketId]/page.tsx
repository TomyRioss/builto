/* eslint-disable @next/next/no-img-element */
import Link from "next/link";
import { ArrowLeft, CalendarDays, UserRound } from "lucide-react";
import { notFound } from "next/navigation";
import { ClarificationButton } from "@/components/admin/admin-action-button";
import { QuoteForm } from "@/components/admin/quote-form";
import { getAdminTicket } from "@/lib/admin/queries";
import { ADMIN_TICKET_STATUS, money } from "@/lib/admin/status";
import { signAttachmentUrls } from "@/lib/storage/ticket-attachments";

export const dynamic = "force-dynamic";
const QUOTABLE = new Set(["PENDING", "CLARIFYING", "QUOTED"]);
const date = new Intl.DateTimeFormat("es-AR", { dateStyle: "medium", timeStyle: "short" });

export default async function AdminTicketDetailPage(props: PageProps<"/admin/tickets/[ticketId]">) {
  const { ticketId } = await props.params;
  const ticket = await getAdminTicket(ticketId);
  if (!ticket) notFound();
  let signed: Record<string, string> = {};
  try { if (ticket.attachments.length) signed = await signAttachmentUrls(ticket.attachments.map((item) => item.storageKey)); } catch (error) { console.error("[admin-ticket] no se pudieron firmar adjuntos", { ticketId, message: error instanceof Error ? error.message : String(error) }); }
  return <div className="mx-auto max-w-[1280px] px-4 py-8 sm:px-6 lg:px-10"><Link href="/admin/tickets" className="inline-flex items-center gap-2 text-sm font-medium text-[#4648d4]"><ArrowLeft className="size-4" />Volver a tickets</Link><header className="mt-7 border-b border-[#e1e3e4] pb-7"><div className="flex flex-wrap items-center gap-3"><span className="rounded-full bg-[#eef0ff] px-2.5 py-1 text-xs font-medium text-[#4648d4]">{ADMIN_TICKET_STATUS[ticket.status]}</span><span className="text-xs text-[#777879]">#{ticket.id.slice(-6).toUpperCase()}</span></div><h1 className="mt-3 text-3xl font-semibold">{ticket.title}</h1><div className="mt-4 flex flex-wrap gap-5 text-sm text-[#666768]"><span className="inline-flex items-center gap-2"><UserRound className="size-4" />{ticket.createdBy.name ?? ticket.createdBy.email}</span><span className="inline-flex items-center gap-2"><CalendarDays className="size-4" />{date.format(ticket.createdAt)}</span><span>Proyecto: {ticket.project.name}</span></div></header><div className="mt-8 grid gap-7 xl:grid-cols-[minmax(0,1.3fr)_minmax(22rem,0.7fr)]"><main className="space-y-7"><section className="rounded-lg border border-[#d9dadb] p-6"><h2 className="text-lg font-semibold">Solicitud del cliente</h2><p className="mt-4 whitespace-pre-wrap text-sm leading-6 text-[#4c4546]">{ticket.description}</p>{ticket.attachments.length > 0 && <div className="mt-6 grid gap-4 sm:grid-cols-2">{ticket.attachments.map((attachment) => signed[attachment.storageKey] ? <a key={attachment.id} href={signed[attachment.storageKey]} target="_blank" rel="noreferrer" className="block overflow-hidden rounded-md border border-[#e1e3e4]">{ }<img src={signed[attachment.storageKey]} alt="Referencia enviada por el cliente" className="aspect-video w-full object-cover" /></a> : <div key={attachment.id} className="grid aspect-video place-items-center rounded-md border border-[#e1e3e4] bg-[#f8f9fa] text-sm text-[#777879]">Imagen no disponible</div>)}</div>}</section><section className="rounded-lg border border-[#d9dadb] p-6"><h2 className="text-lg font-semibold">Historial de cotizaciones</h2>{ticket.quotes.length === 0 ? <p className="mt-4 text-sm text-[#666768]">Todavia no se emitieron cotizaciones.</p> : <div className="mt-4 overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="border-b border-[#e1e3e4] text-xs uppercase text-[#777879]"><tr><th className="py-3">Fecha</th><th>Importe</th><th>Tiempo</th><th>Estado</th><th>Notas</th></tr></thead><tbody className="divide-y divide-[#eceeef]">{ticket.quotes.map((quote) => <tr key={quote.id}><td className="py-4">{date.format(quote.createdAt)}</td><td>{money(quote.amount, quote.currency)}</td><td>{quote.estimatedDays ? `${quote.estimatedDays} dias habiles` : "-"}</td><td>{quote.status}</td><td className="max-w-56 truncate">{quote.notes ?? "-"}</td></tr>)}</tbody></table></div>}</section></main><aside className="space-y-7"><section className="rounded-lg border border-[#d9dadb] p-6"><h2 className="text-lg font-semibold">Gestion</h2>{ticket.status === "PENDING" && <div className="mt-4"><ClarificationButton ticketId={ticket.id} /></div>}{QUOTABLE.has(ticket.status) ? <div className="mt-5"><QuoteForm ticketId={ticket.id} /></div> : <p className="mt-4 text-sm leading-6 text-[#666768]">Este ticket ya avanzo y no admite una nueva cotizacion en su estado actual.</p>}</section><section className="rounded-lg border border-[#d9dadb] p-6"><h2 className="text-lg font-semibold">Ejecucion</h2><dl className="mt-4 space-y-4 text-sm"><div><dt className="text-[#777879]">Developer</dt><dd className="mt-1 font-medium">{ticket.assignedDev?.name ?? ticket.assignedDev?.email ?? "Sin asignar"}</dd></div><div><dt className="text-[#777879]">Pagos registrados</dt><dd className="mt-1 font-medium">{ticket.transactions.length}</dd></div></dl></section></aside></div></div>;
}
